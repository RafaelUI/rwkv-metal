# NEXT_SESSION — rwkv-metal

Записка по одной теме. Остальное состояние репозитория — в git-истории и в
README; здесь только то, что открыто прямо сейчас.

## CAST_WKV_OUTPUT — течь fp32 из ядра WKV (03.08)

### Что происходит

Ядро WKV считает рекуррентность в fp32 — иначе нельзя, это условие
сходимости пошагового пути со сплошным. Но оно и ВОЗВРАЩАЕТ fp32, и этот тип
уходит в остаточный поток: ln_x, bonus, o_proj, следующий слой, голова.
Веса при этом bf16, поэтому каждый матмул поднимает ВЕСЬ вес до fp32.

Течь начинается в tmix СЛОЯ 0: вход туда ещё bf16, выход уже fp32.
`body()` возвращает fp32 на любой модели.

### Правка

`rwkv_metal/model/rwkv7_x070.py`, модульный флаг `CAST_WKV_OUTPUT`,
по умолчанию **False**. Одна строка после вызова ядра:

```python
if CAST_WKV_OUTPUT:
    out = out.astype(x.dtype)
```

Приводится ВЫХОД, не рекуррентность: `h_out` остаётся fp32 всегда. Флаг
читается на каждом проходе, поэтому обе ветки сравниваются в одном процессе.

### Замеры

`tests/dev_wkv_cast_ab.py <pth> [n_chunks] [speed|ppl|both]`. Режимы разделены
намеренно: ppl-фаза держит логиты `[T, 65536]` в fp32 (134 МБ на чанк при
T=512) и на 1.5B уводит машину в своп. Своп не портит ppl — число
детерминированное, — но портит любой замер времени в том же процессе.

**0.1B (L=12), своп не двигался, A/B чередованием:**

| | выкл | вкл | |
|---|---|---|---|
| декод | 15.81 мс/ток | **5.69** | **×2.78** |
| ppl (test.txt, 8176 позиций) | 18.2512 | 18.2691 | **+0.098%** |

Разбросы 15.9/15.8/15.9/15.7/15.8 против 5.7/5.7/5.7/5.7/5.7 — эффект на
порядок больше шума. Контрольный ppl-повтор совпал знак в знак.

**1.5B (L=24), ppl достоверна, тайминг НЕТ:**

| | выкл | вкл | |
|---|---|---|---|
| ppl (6132 позиции) | 8.9483 | 8.9499 | **+0.018%** |
| декод | 161–201 мс/ток | 41–73 | ×2.75…3.92 — НЕ ЦИТИРОВАТЬ |

Тайминг недействителен: 3 ГБ весов на 16 ГБ при уже занятых 3 ГБ свопа,
разбросы 190–216 мс против 15.7–15.9 на 0.1B. Направление устойчиво (оба
прогона дали кратный выигрыш), величина — нет. Мерить на машине посвободнее
или на квантованной базе.

### Главный вывод, и он контринтуитивен

**Цена по качеству ПАДАЕТ с глубиной, а не растёт.**

| | слоёв | ppl-цена |
|---|---|---|
| 0.1B | 12 | +0.098% |
| 1.5B | 24 | **+0.018%** |

Ожидание было обратным: округлений bf16 в остаточном потоке по одному на
слой, на 24 слоях их вдвое больше. Правдоподобное объяснение — 1.5B просто
увереннее (ppl 8.95 против 18.25), распределения острее, и та же добавка к
логитам двигает лосс слабее. Но это ОБЪЯСНЕНИЕ ПОСЛЕ ФАКТА; проверять
гипотезу отдельно никто не проверял.

Практический смысл: чем крупнее модель, тем безболезненнее правка. Для
масштаба — пресет REDUCTION в rwkv-quant стоит +0.12% и описан там как
«деградация около нуля, для QAT/QLoRA-базы».

### Зачем это здесь, а не только в SwiftRWKV

SwiftRWKV — порт этого репозитория, и rwkv-metal для него ИСТОЧНИК ЭТАЛОНОВ
паритета. Та же правка сделана там (`X070Backbone.castWKVOutputToComputeDType`,
тоже за флагом, тоже выключена). Если включить только с одной стороны,
паритет развалится не потому, что порт неправ, а потому, что он стал
отличаться от источника. Включать — согласованно.

Кросс-проверка двух независимых реализаций на 0.1B:

| | декод | ppl |
|---|---|---|
| rwkv-metal (Python) | ×2.78 | +0.098% |
| SwiftRWKV | ×2.82 | +0.090% |

### Что осталось

- Тайминг 1.5B на машине без свопа.
- 2.9B — есть только в `.rwkvq` (`~/Develop/Synapse/Models/`), плотных
  весов нет; через квантованный путь замерить можно, но это отдельная
  обвязка.
- Решение о включении по умолчанию: после того, как в SwiftRWKV пересмотрят
  допуски паритета. Числа для решения собраны, не хватает только самого
  решения.
